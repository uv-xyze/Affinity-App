<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.affinity_projectfinal">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Affinity_projectFinal."
        tools:targetApi="31">

        <activity
            android:name=".CourseHomeActivity"
            android:exported="false" />
        <activity
            android:name=".ConnectionsActivity"
            android:exported="true">
            <!-- Optionally add intent filters here if this activity is started with specific intents -->
        </activity>
        <activity
            android:name=".ChatActivity"
            android:exported="true">
            <!-- Optionally add intent filters here if this activity is started with specific intents -->
        </activity>
        <activity
            android:name=".RelationshipAAQ2"
            android:exported="false" />
        <activity
            android:name=".AttachmentAnalysisTest"
            android:exported="false" />
        <activity
            android:name=".EditProfileActivity"
            android:exported="false" />

        <!-- SplashActivity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AuthActivity -->
        <activity
            android:name=".AuthActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" />

        <!-- SignupActivity -->
        <activity
            android:name=".SignupActivity"
            android:exported="false" />

        <activity
            android:name=".ProfileActivity"
            android:exported="false" />
    </application>

</manifest>
